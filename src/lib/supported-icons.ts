export type IconName = "kitty"; // Add more icon names as needed

import kitty from "@/assets/icons/kitty.webp";

const icons: Record<IconName, any> = {
  kitty, // Custom icon for ticket
};

export { icons };
